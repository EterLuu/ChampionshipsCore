package ink.ziip.championshipscore.protocol;

/** The two pre-game viewing modes supported by Core's shared introduction lifecycle. */
public enum BingoIntroductionMode {
    ADVENTURE,
    SPECTATOR
}
