package ink.ziip.championshipscore.protocol;

/** Logical Bingo dimension, independent of the physical world names chosen by a worker. */
public enum BingoDimension {
    OVERWORLD,
    NETHER,
    THE_END
}
